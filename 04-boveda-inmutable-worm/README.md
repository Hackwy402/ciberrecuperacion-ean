# Sesión 4 · Bóveda inmutable (WORM)

> 🔜 Contenido disponible antes de la sesión.

**MinIO** con S3 **Object Lock (WORM)**, repositorios append-only en
**restic/BorgBackup** y snapshots **ZFS**. **Lab:** ataque a la bóveda y validación
con **AIDE** y **Wazuh FIM**.

Usa el mismo entorno de `../core` (aquí se suman servicios con `docker compose`).
