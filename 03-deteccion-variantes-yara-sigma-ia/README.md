# Sesión 3 · Detección de variantes asistida por IA

> 🔜 Contenido disponible antes de la sesión.

Reglas **YARA** y **Sigma** generadas con un LLM local desde informes CTI y
validadas con `yara`/`sigma-cli`. **Lab:** del reporte de amenaza a la regla funcional.

Usa el mismo entorno de `../core`.
